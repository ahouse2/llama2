{{- if and .Values.backend.enabled .Values.backend.serviceAccount.create -}}
{{- $ns := default .Release.Namespace .Values.namespace.name -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (printf "%s-backend" (include "discovery-platform.fullname" .)) .Values.backend.serviceAccount.name }}
  namespace: {{ $ns }}
  labels:
    {{- include "discovery-platform.labels" (dict "context" . "component" "backend") | nindent 4 }}
  {{- with .Values.backend.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end -}}
