{{- if and .Values.worker.enabled .Values.worker.service.enabled -}}
{{- $ns := default .Release.Namespace .Values.namespace.name -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-worker" (include "discovery-platform.fullname" .) }}
  namespace: {{ $ns }}
  labels:
    {{- include "discovery-platform.labels" (dict "context" . "component" "worker") | nindent 4 }}
spec:
  type: {{ .Values.worker.service.type }}
  selector:
    {{- include "discovery-platform.selectorLabels" (dict "context" . "component" "worker") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.worker.service.port }}
      targetPort: http
{{- end -}}
